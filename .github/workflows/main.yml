on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: Find merge conflicts
    steps:
      - uses: actions/checkout@v1
      - name: Find Merge Conflict finder
        id: conflict
        uses: olivernybroe/action-conflict-finder@master
